export interface INewUserData {
  addNew: boolean;
  isProfile: boolean;
  user: IUser;
}

export interface ISelectionOption {
  isAdministrator: boolean;
  isOperator: boolean;
  isActive: boolean;
}
